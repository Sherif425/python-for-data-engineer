[project]
name = "python-for-data-engineer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sherif Mohamed", email = "sherif425@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python-for-data-engineer = "python_for_data_engineer:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
